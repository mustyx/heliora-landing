# Sensitive config — NEVER push these
config/config.php

# Temporary diagnostic that exercises the Zoho OAuth chain. Deliberately
# untracked: if it were committed, deleting it from the server would be
# undone by the next deploy, and an endpoint that touches the CRM token
# should not be able to resurrect itself. Upload by hand, delete by hand.
zoho-check.php
.env
.env.*
*.env

# SSH deploy keys — NEVER push these
heliora_deploy_key
heliora_deploy_key.pub

# Logs
*.log
logs/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.vscode/
.idea/

# Node (if ever used locally)
node_modules/

# Local preview / design scratch — never deploy
start-preview.bat
index-prerebrand-backup.html
designs/
.impeccable/
.claude/
*.jpeg
push-live.bat
